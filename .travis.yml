language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

install:
  - pip install pytest pytest-cov python-coveralls coverage

script:
  - python setup.py install
  - pytest --cov=systemrdl

after_success:
  - coveralls